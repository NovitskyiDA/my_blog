<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @post.body %>
</p>

<p>
  <strong>Tag:</strong>
  <%= @post.tags %>
</p>

<%= (link_to 'Edit', edit_post_path(@post) if @post.user.id == current_user.id) if current_user %>
<%= link_to 'Back', posts_path %>
